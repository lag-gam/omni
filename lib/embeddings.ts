// Phase 4: generate an embedding per note on save, and a brute-force
// cosine-similarity search over stored embeddings at query time.
//
// export async function embed(text: string): Promise<number[]> { ... }
// export function cosineSimilarity(a: number[], b: number[]): number { ... }
// export function topMatches(query: number[], notes: Note[], k = 5): Note[] { ... }

export {};
